import React from "react";

export const Footer = () => {
    return(
        <footer>
            <label className="footer-label">Website created as a rerun project</label>
        </footer>
    );
};